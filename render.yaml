services:
  - type: web
    name: roadaid-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn vehicle_api.wsgi:application --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: DEBUG
        value: false
